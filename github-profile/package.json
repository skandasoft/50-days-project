{
  "name": "@skandasoft/github-profile",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "browserslist": [
    "since 2017-06"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx parcel@2.0.0-beta.3 build src/index.html --no-cache --no-source-maps --public-url ./",
    "start": "npx parcel src/index.html -p 80 --no-cache --dist-dir dist "
  },
  "author": "SkandaSoft <skandasoftinc@gmail.com> (http://skandasoft.com/)",
  "license": "MIT",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-beta.3",
    "autoprefixer": "^10.3.1",
    "cssnano": "^5.0.6",
    "parcel": "^2.0.0-beta.3",
    "postcss": "^8.3.5"
  },
  "dependencies": {
    "axios": "^0.21.1"
  },

  "comments": {
    "build": "parcel build src/index.html --no-cache --no-source-maps --public-url ./",
    "start": "parcel src/index.html -p 80 --no-cache --dist-dir dist ",
    "npm-details": "try without installing locally???"
  }
}
